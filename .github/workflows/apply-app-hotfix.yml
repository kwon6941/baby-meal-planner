name: Apply app hotfix

on:
  push:
    paths:
      - '.github/workflows/apply-app-hotfix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install updater dependencies
        run: pip install -r requirements-updater.txt
      - name: Expand recipe database to target size
        run: python tools/update_recipes.py
      - name: Commit changes
        run: |
          git config user.name "recipe-db-bot"
          git config user.email "recipe-db-bot@users.noreply.github.com"
          git add tools/update_recipes.py recipes.json
          if git diff --cached --quiet; then
            echo "No changes needed"
          else
            git commit -m "feat: expand toddler recipe database toward 300"
            git push
          fi

# expansion trigger: 2026-09-14-v3-parallel
